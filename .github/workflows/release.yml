name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
          - os: macos-latest
            target: x86_64-apple-darwin
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: windows-latest
            target: x86_64-pc-windows-msvc

    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Build
        shell: bash
        run: |
          cargo build --release --target ${{ matrix.target }}
          mkdir -p dist
          BIN="datastar-lsp"
          if [[ "${{ matrix.target }}" == *windows* ]]; then
            BIN="datastar-lsp.exe"
          fi
          EXT=""
          if [[ "${{ matrix.target }}" == *windows* ]]; then
            EXT=".exe"
          fi
          cp "target/${{ matrix.target }}/release/$BIN" "dist/datastar-lsp-${{ matrix.target }}$EXT"
          strip "dist/datastar-lsp-${{ matrix.target }}$EXT" || true

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.target }}
          path: dist/datastar-lsp-${{ matrix.target }}*

  vsix:
    name: Build VSIX
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install deps & build
        run: |
          cd vscode
          bun install
          VERSION=$(grep '^version' ../Cargo.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          bunx --bun vsce package -o datastar-lsp-$VERSION.vsix
          mkdir -p ../dist
          mv datastar-lsp-$VERSION.vsix ../dist/

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: vsix
          path: dist/datastar-lsp-*.vsix

  release:
    name: Create Release
    needs: [build, vsix]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v8
        with:
          path: dist

      - name: Rename binaries
        run: |
          mv dist/x86_64-unknown-linux-gnu/datastar-lsp-x86_64-unknown-linux-gnu dist/datastar-lsp-linux-x64
          mv dist/aarch64-unknown-linux-gnu/datastar-lsp-aarch64-unknown-linux-gnu dist/datastar-lsp-linux-arm64
          mv dist/x86_64-apple-darwin/datastar-lsp-x86_64-apple-darwin dist/datastar-lsp-darwin-x64
          mv dist/aarch64-apple-darwin/datastar-lsp-aarch64-apple-darwin dist/datastar-lsp-darwin-arm64
          mv dist/x86_64-pc-windows-msvc/datastar-lsp-x86_64-pc-windows-msvc.exe dist/datastar-lsp-windows-x64.exe
          rm -rf dist/x86_64-unknown-linux-gnu dist/aarch64-unknown-linux-gnu
          rm -rf dist/x86_64-apple-darwin dist/aarch64-apple-darwin dist/x86_64-pc-windows-msvc

      - name: Download VSIX
        uses: actions/download-artifact@v8
        with:
          name: vsix
          path: dist

      - name: Release
        uses: softprops/action-gh-release@v3
        with:
          files: dist/*
          generate_release_notes: true
