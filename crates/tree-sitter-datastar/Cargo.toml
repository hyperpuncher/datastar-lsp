[package]
name = "tree-sitter-datastar"
version = "0.1.0"
edition = "2021"
description = "Tree-sitter grammar for Datastar expressions and attributes"

[dependencies]
tree-sitter = "0.26"

[build-dependencies]
cc = "1"

[lib]
path = "src/lib.rs"
